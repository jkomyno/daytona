version: '2'
run:
  build-tags:
    - testing
linters:
  enable:
    - forbidigo
  settings:
    errcheck:
      exclude-functions:
        - (*github.com/gin-gonic/gin.Context).Error
        - io.Copy
        - syscall.Syscall
        - (github.com/gliderlabs/ssh.Session).Exit
        - (io.Writer).Write
    forbidigo:
      forbid:
        - pattern: '\.AbortWithError\('
          msg: 'use c.Error(common_errors.NewXxxError(...)) instead of c.AbortWithError'
        - pattern: '\.AbortWithStatusJSON\('
          msg: 'use c.Error(common_errors.NewXxxError(...)) instead of c.AbortWithStatusJSON'
  exclusions:
    generated: lax
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling
    paths:
      - third_party$
      - builtin$
      - examples$
formatters:
  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$
